
# Multimedia Elements in HTML

## 📘 Overview
This section covers HTML multimedia elements used to embed audio, video, and external content into web pages. Multimedia elements enhance user experience by allowing rich media content directly in the browser.

## 🧩 Topics Covered
- Audio element (`<audio>`)
- Video element (`<video>`)
- Source element (`<source>`)
- Media attributes (controls, autoplay, loop, muted)
- Iframe element (`<iframe>`)

## 📂 Files in This Folder
- `audio.html` – Demonstrates how to embed audio files
- `video.html` – Demonstrates how to embed video files
- `iframe.html` – Demonstrates embedding external content such as websites or videos

## 🎵 Audio Element
The `<audio>` tag is used to embed sound content such as music or podcasts.

**Key attributes:**
- `controls`
- `autoplay`
- `loop`
- `muted`

## 🎬 Video Element
The `<video>` tag is used to embed video content.

**Key attributes:**
- `controls`
- `width` / `height`
- `autoplay`
- `loop`
- `muted`

## 🌐 Iframe Element
The `<iframe>` tag is used to embed another webpage or external content inside the current page.

**Common uses:**
- Embedding websites
- Maps
- Videos (e.g., YouTube)

## ✅ Best Practices
- Always include `controls` for accessibility
- Provide fallback text for unsupported browsers
- Use supported media formats (MP3, MP4, WebM)
- Avoid autoplay unless necessary
- Optimize media files for performance

## 🎯 Learning Outcome
After completing this topic, you will be able to:
- Embed audio and video in HTML pages
- Use media attributes effectively
- Embed external content using iframes

---

📌 **Note:** Media files should be placed in the same folder or properly linked using relative paths.
